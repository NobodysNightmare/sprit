default: &default
  adapter: mysql2
  username: root

development:
  <<: *default
  database: sprit_development
  host: 127.0.0.1

test:
  <<: *default
  database: sprit_test
  host: 127.0.0.1

production:
  adapter: mysql2
  database: sprit
  username: "<%= ENV["SQL_USER"] %>"
  password: "<%= ENV["SQL_PASSWORD"] %>"
